import { Meta, StoryObj } from "@storybook/react";
import { PageLayout } from "./PageLayout";

const meta: Meta<typeof PageLayout> = {
  title: "Layout/PageLayout",
  component: PageLayout
};

export default meta;
export const Layout: StoryObj<typeof PageLayout> = {};
